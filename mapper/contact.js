// async function getProfile (db) {
//   let profile = await db.get('SELECT * FROM contact WHERE self = 1 LIMIT 1');
//   return profile;
// }

// module.exports = { initContact, updateContact, getProfile, updateProfile };
